# App Store Screenshots

The exact screenshots currently retained in App Store Connect are backed up in
`iphone-6.5/app-store-current` in this order:

1. `01-home.png`
2. `02-ai-helper.png`
3. `03-plus-workspace.png`
4. `04-file-vault.png`
5. `05-tps-renewal.png`
6. `06-resources.png`
7. `07-privacy-and-safety.png`

Each image is `1242 x 2688` pixels. This directory is a recovery reference only:
do **not** submit it with the rebuilt binary. Screenshots 02 and 03 still use the
old "AI Helper" name, and screenshot 03 reflects checklist totals from the lost
build rather than the reconstructed source. Capture a fresh seven-image set from
the final TestFlight build before App Review. The `final` directory contains the
earlier five-image set and is also kept only for history.

The source captures are kept one directory above the `final` folder for future updates.
